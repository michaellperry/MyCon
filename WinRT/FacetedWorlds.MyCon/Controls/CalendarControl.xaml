<UserControl
    x:Class="FacetedWorlds.MyCon.Controls.CalendarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FacetedWorlds.MyCon.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="127"
    d:DesignWidth="125">

    <Grid
        Background="#FF852B2B" Width="125" Height="125">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock
            Text="{Binding Day}"
            FontSize="26.667"
            Margin="12,12,12,6"
            HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBlock
            Grid.Row="1"
            Text="{Binding Time}"
            FontSize="26.667"
            Margin="12,6,12,12"
            HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</UserControl>
