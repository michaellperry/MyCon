@model FacetedWorlds.MyCon.Web.ViewModels.TrackDetailsViewModel

@{
    ViewBag.Title = @Model.Name;
}

<h2>@Model.Name</h2>

@foreach (var session in @Model.Sessions)
{
    <div class="session">
        <div class="calendar">
            <p>@session.Day</p>
            <p>@session.Time</p>
        </div>
        <h3>@session.Name</h3>
        <p>@session.Speaker</p>
        <p>Room: @session.Room Track: @session.Track</p>
        <p>@session.Description</p>
        <div class="clear"></div>
    </div>
}